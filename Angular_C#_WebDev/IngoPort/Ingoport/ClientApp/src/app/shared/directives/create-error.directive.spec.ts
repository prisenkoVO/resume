import { CreateErrorDirective } from './create-error.directive';

describe('CreateErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateErrorDirective();
    expect(directive).toBeTruthy();
  });
});
